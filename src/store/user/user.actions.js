// import { createAsyncThunk } from "@reduxjs/toolkit";

// const fetchUserId = (userId) =>
//   new Promise((resolve) =>
//     setTimeout(() => {
//       resolve({ id: 1, name: "Vlad" });
//     }, 1000)
//   );

// export const getUserById = createAsyncThunk("users/by-id", async (userId) => {
//   const response = await fetchUserId(userId);
//   return response;
// });
